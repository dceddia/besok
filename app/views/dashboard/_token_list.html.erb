<span>Click one to see more information</span>
<ul class="token-list">
  <% @tokens.each do |token| %>
  <a href="<%= token_path(token) %>">
    <li>
      <div class="views">
        <div><%= token.visits.count %></div>
        views
      </div>
      <pre><%= token.name %></pre>
    </li>
  </a>
  <% end %>
</ul>